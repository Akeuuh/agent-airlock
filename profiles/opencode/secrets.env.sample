# Clés d'auth du profil opencode (mode apikey). Copie ce fichier en secrets.env
# (gitignoré) et renseigne les valeurs ; seules les clés listées dans
# HARNESS_ENV_KEYS (profile.env) franchissent la frontière du conteneur.
ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
